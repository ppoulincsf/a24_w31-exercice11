#include "stdafx.h"
#include "CppUnitTest.h"
#include "Queue.h"
using namespace Microsoft::VisualStudio::CppUnitTestFramework;

namespace TestsCode
{
  TEST_CLASS(QueueTests)
  {
  public:

    TEST_METHOD(Contructor)
    {
      // PROF: Décommentez ce test et complétez les autres tests

    /*
      Queue queue;

      Assert::IsTrue(queue.isEmpty());
      Assert::AreEqual((unsigned int)0, queue.size());
    */

    }
  };
}